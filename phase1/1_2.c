/*
 * CHALLENGE: SWAP Meet
 * Write a program that:
 * 1. Takes two integers from the user.
 * 2. Prints them before swap.
 * 3. Swaps their values using a third temporary variable.
 * 4. Prints them after swap.
 */
#include <stdio.h>

int main() {
int a, b, temp;
printf("Enter integer a:");
scanf("%d", &a);
printf("Enter integer b:");
scanf("%d", &b);
    return 0;
}